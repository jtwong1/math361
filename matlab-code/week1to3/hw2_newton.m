function [x, it, seq] = hw2_newton(f, df, x, tol, max_iter)
%  ...
% Inputs:
%       ...

% Returns:
%    x: the approximate root
%    it: the number of iterations taken
%   seq: the sequence of iterates

it = 0;
seq = [1, 2, 3, 4];
    
while(false)
    %...
end
        